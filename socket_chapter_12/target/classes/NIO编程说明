NIO：
非阻塞io,面向缓冲区,效率高
NIO通道（传输数据）+缓冲区(存放数据) +直接/非直接缓冲区
非阻塞IO和阻塞IO区别
同步：直连
异步：非直连
阻塞概念:应用程序在获取网络数据的时候,如果网络传输很慢,那么程序就一直等着,直接到传输完毕。
非阻塞概念：应用程序直接可以获取已经准备好的数据,无需等待.
IO为同步阻塞形式,NIO为同步非阻塞形式。NIO没有实现异步,在JDK1.7之后，升级了NIO库包
,支持异步费阻塞通讯模型NIO2.0(AIO)

IO(BIO)同步，阻塞IO,NIO同步,非阻塞IO,AIO(异步,非阻塞IO)
多线程（伪异步阻塞io）---》》无真正解决阻塞io核心。
NIO:同步非阻塞，
客户端请求注册到selector(选择器)上，由选择器来通知服务器端，做非阻塞io，数据在准备之前服务器无需做等待
